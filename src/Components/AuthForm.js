import styled from 'styled-components';

const AuthFormSample = styled.div`
`;

const AuthForm = () => {
    return(
        <AuthFormSample>
            AuthForm
        </AuthFormSample>
    );
};

export default AuthForm;